<!-- dpyc-awaiting-funds -->
⏳ **Deferred — the factory is out of LLM credits.** This run died because the shared LLM-router key was exhausted or capped (HTTP 402 — either a hard reject before inference, or a mid-run refuse after cheap early turns). It is tagged `awaiting-funds`; the credit canary clears the tag and re-runs the agent automatically once funding is restored — nothing here is skipped for good. Tracking: lonniev/dpyc-community#102.
